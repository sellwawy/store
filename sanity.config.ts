import { defineConfig } from 'sanity'

export default defineConfig({
   projectId: 'lz1bac5t',
   dataset: 'production',
   title: 'E-commerce Studio',
   apiVersion: '2023-12-05', // Update to today's date or the correct version.
   plugins: [],
})
